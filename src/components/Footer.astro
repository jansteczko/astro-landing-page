---
import siteData from '../data/site.json'
import Stopka from '../components/Footer1.astro'

const pn = Astro.url.pathname
const isHome = pn === '/'
const site = Astro.site
const abtLink = isHome
  ? '#about'
  : `${site}#about`
const ftLink = isHome
  ? '#features'
  : `${site}#features`
---
<Stopka/>


